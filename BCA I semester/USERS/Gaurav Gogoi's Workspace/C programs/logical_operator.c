#include<stdio.h>
void main(){
    // This program uses te logical operator && which requires both its operands to be true for the final result as true or 1
    int i=1;
    int j=1;
    printf("%d \n", (i&&j));
    printf("%d \n", (i||j));
    printf("%d \n", !(i=j));

    
}
    