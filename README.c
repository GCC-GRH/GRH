#include<stdio.h>
int main (){
  int a,b,c;
  printf("enter the first no.:");
  scanf("%d",&a);
  printf("enter the second no.:");
  scanf("%d",&b);
  int c=a+b;
  printf("The sum of the no. is %d",sum=a+b);
  scanf("%d",&c);
  return 0
}
