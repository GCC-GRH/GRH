# The range(start value (optional), end value (mandatory), multiple iterator at an iteration step (optional)) function is used as a predefined set of tuple which can be used as a pool of targetted values
for i in range(0, 10) :
    print(i,end="")